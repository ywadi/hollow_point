# Project overview

**HollowPoint** is a **general-purpose 3D game engine** in C++20, built to power
**several** games for a small studio.

That sentence is the load-bearing one. The engine is a reusable library, and a
game is a **separate project** — its own assets, scenes and gameplay module,
built against an installed engine — not something that lives in this repository.
`samples/sandbox/` is the engine's own sample module, **not "the game"** (T0109).

This decides what a ticket is allowed to conclude. An engine capability is worth
building or it is not, and that is answered by cost, by schedule, and by whether
it is foundational — never by what a hypothetical game turns out to need. T0044
("define the game") was written on the opposite premise and was dropped for it;
T0126 removed the framing it had spread through the backlog.

Rendering is on [DiligentEngine](https://github.com/DiligentGraphics/DiligentEngine).
Git repository at `github.com/ywadi/hollow_point`. Everything under
`third_party/` is a submodule, so clone with `--recursive` — or run
`git submodule update --init --recursive`, because `git pull` does not fetch
them.

## Tree

```
CMakeLists.txt              root build; third_party, engine, samples, apps, tests
build.zig                   the build harness -- single entry point, both hosts
bootstrap.sh / .ps1         install pinned zig + cmake + ninja into .harness/
BUILDING.md                 user-facing build instructions
engine/
  include/hp/               the public API -- what gameplay code compiles against
  src/                      the engine shared library
  module/                   objects compiled *into* every gameplay module
apps/
  editor/                   hp_editor -- a consumer of the engine, gone on export
  runtime/                  hp_runtime -- what ships with a game
samples/sandbox/            the engine's own sample gameplay module (not a game)
cmake/
  toolchains/               zig toolchain files (see 03-build-harness.md)
  dist.cmake                stages a built tree into dist/
  hp_build_id.cmake         mints the engine build id (T0104)
  HpGameplayModule.cmake    hp_add_gameplay_module() -- the only way to declare one
docs/api/                   generated API reference, regenerated by the build
claude_documentation/       the backlog, the decision log, and these documents
tools/
  gen_api_docs.py           regenerates docs/api
  check_backlog.py          folder/Status/board consistency for the backlog
  mk_linux_sysroot.sh       regenerates the Linux stub sysroot
  find_win_header_case.sh   rescans for case-mismatched Windows headers/libs
  harness/                  harness logic split out so tests can import it
third_party/
  DiligentEngine/           rendering (submodules: Core, Tools, Samples, FX)
  SDL/                      SDL3 -- window, input, haptics
  entt/                     ECS *and* the reflection system (T0053)
  imgui/                    ocornut/imgui `docking` -- overrides Diligent's copy
  doctest/ abseil-cpp/ rapidjson/
  enkiTS/ meshoptimizer/ ozz-animation/
  ufbx/                     v0.23.0 -- present but NOT wired into the build
  dxc/                      v1.8.2407 binaries -- present but NOT wired in
  sysroot/linux-x86_64/     X11/xcb/GL headers + generated link stubs
tests/
  fast/ integration/        C++ suites (doctest), one binary per bucket
  fixtures/                 module fixtures the boundary suite loads
  harness/                  Zig suites covering the build harness itself
.harness/<tool>/<host>/     bootstrapped toolchain, keyed by host (gitignored)
build/<target>-<config>/    build trees
dist/<target>/              staged output
```

## What the build produces

Four artifacts that matter, out of roughly 1100 build targets per platform:

| Artifact | What it is |
|---|---|
| `libhp_engine.so` / `.dll` | the engine, a **shared** library (D12) — one copy per process, so engine state exists exactly once |
| `hp_editor` | the editor, a consumer of the engine; disappears when a game is exported |
| `hp_runtime` | what ships with a game |
| `libhp_sandbox.so` / `.dll` | the sample gameplay module, loaded at run time |

Plus Diligent's `GraphicsEngineVk` / `GraphicsEngineOpenGL` shared libraries.

`zig build dist` stages a runnable tree into `dist/<target>/`: executables and
the libraries they load in `bin/`, shared and static libraries in `lib/`. An
exported build runs from a directory it was not built in with the build tree
gone — measured on both targets, see T0105.4.

Our code is **C++20**; Diligent builds at 17 alongside it.

## Graphics backends

| Target | Backends |
|---|---|
| Linux x86_64 | Vulkan, OpenGL |
| Windows x86_64 | Vulkan, OpenGL |

**No Direct3D on Windows**, and this is by design, not an oversight. Zig targets
Windows through the MinGW-w64 ABI, MinGW has no `atlbase.h`, and DiligentCore
gates D3D11/D3D12 on ATL (`DiligentCore/CMakeLists.txt:160-181`). The engine
detects this and configures itself for Vulkan + GL. Direct3D would require the
MSVC ABI plus a real Windows SDK, which cannot be driven from a Linux host.
The user chose this trade-off explicitly.

## History worth knowing

- There was an app at `apps/terrain_lab` (a derivative of Diligent's Atmosphere
  sample plus custom `Water/WaterLayer.cpp` and `Water/TerrainAS.cpp`). The user
  **deleted it deliberately** and asked for all traces removed; `build/apps` was
  removed accordingly. Its custom shader headers and terrain assets existed only
  inside the build tree and are gone. This is expected, not an accident.
- There was briefly a `game/` directory. It is gone, because a game is a
  separate project; `samples/sandbox/` is what stands in its place. A build tree
  from before the change may still carry `build/<target>/game/` — that is an
  orphan of a deleted source directory, not output, and `dist` cannot tell the
  difference (see T0105's notes).
- `ufbx` and `third_party/dxc` are staged for future use and referenced by
  nothing. Do not assume they work.
