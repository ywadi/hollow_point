# hp_editor

The HollowPoint editor. A consumer of the engine library (`hp::engine`), not
part of it — see T0013 and D12.

Skeleton only: it links the engine and reports what it sees. The application
shell and main loop are T0014, and the window and platform layer T0015.
